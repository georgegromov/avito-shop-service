# 📦 Avito Shop Service

Сервис внутреннего магазина мерча, в котором сотрудники могут покупать товары за монетки (coin) и
переводить их друг другу. Каждый новый пользователь автоматически получает кошелёк с балансом 1000
coin.

## 🚀 Функциональные возможности

- Регистрация и авторизация пользователей
- Автоматическое создание кошелька при регистрации
- Покупка мерча:
  - проверка баланса
  - атомарная транзакция (списание монет → запись покупки)
- Перевод монет другим сотрудникам:
  - списание, начисление и запись transfer выполняются атомарно
- История покупок
- История переводов:
  - входящие (кто прислал)
  - исходящие (кому отправил)

## Запуск через Docker Compose

```bash
# Собрать и запустить сервис:
docker-compose up --build

# Приложение будет доступно по адресу:
http://localhost:8080
```

## Локальный запуск без Docker

```bash
go mod download
go run ./cmd/avito-shop-service/main.go
```

## 🛠️ Стек технологий

Backend:

- Golang 1.24.4
- DDD + Layered Architecture (domain / repository / service / handler)
- Gin Framework
- JWT
- bcrypt
- slog
- sqlx

Хранилище:

- PostgreSQL 17

Инфраструктура:

- Docker
- docker-compose
- Viper
- Makefile

## 📁 Структура проекта

```
.
├── cmd/
│   └── avito-shop-service/     # Точка входа
├── internal/
│   ├── app/           			# Внедрение зависимостей
│   ├── domain/           	# Сущности и интерфейсы
│   ├── service/          	# Бизнес-логика
│   ├── repository/       	# PostgreSQL-репозитории
│   ├── handler/          	# Http-обработчики
│   └── infra/
│       ├── security/     	# Jwt и Hash менеджеры
│       ├── server/       	# Http сервер
│       └── postgres/     	# Подключение к бд
├── pkg/
│   ├── config/           	# Viper
│   ├── hash/             	# HashManager
│   ├── jwt/          			# JwtManager
│   ├── hash/             	# HashManager
│   ├── server/          		# HttpServer
│   ├── utils/
│   └── validator/
├── docker-compose.yml
├── Dockerfile
├── .env
└── README.md
```

## ⚙️ Пример конфигурации (.env.example)

```
ENV=example

# HTTP
HTTP_HOST=
HTTP_PORT=
HTTP_READ_TIMEOUT=
HTTP_WRITE_TIMEOUT=
HTTP_IDLE_TIMEOUT=
HTTP_MAX_HEADER_BYTES=

# PostgreSQL
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USERNAME=
POSTGRES_PASSWORD=
POSTGRES_DBNAME=
POSTGRES_SSLMODE=

# JWT
JWT_SECRET=
JWT_TOKEN_TTL=

# Hash
HASH_COST=
```

## Основные эндпоинты

| Метод | URL          | Описание          |
| ----- | ------------ | ----------------- |
| POST  | /auth/signin | Регистрация       |
| POST  | /auth/signup | Авторизация       |
| POST  | /purchases   | Покупка товара    |
| GET   | /purchases   | История покупок   |
| POST  | /transfers   | Перевод монет     |
| GET   | /transfers   | История переводов |
