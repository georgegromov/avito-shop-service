name: avito-shop-service

services:
  avito_shop_service:
    build: .
    container_name: avito_shop_service
    ports:
      - "8080:8080"
    depends_on:
      - avito_shop_migrate
    networks:
      - internal
  avito_shop_database:
    image: postgres:17
    env_file: .env
    container_name: avito_shop_database
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DBNAME}
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - internal
  avito_shop_migrate:
    image: migrate/migrate
    container_name: avito_shop_migrate
    depends_on:
      - avito_shop_database
    volumes:
      - ./migrations:/migrations
    command:
      [
        "-path",
        "/migrations",
        "-database",
        "postgres://postgres:postgres@avito-shop-database:5432/avito_shop?sslmode=disable",
        "up",
      ]
    networks:
      - internal
    restart: "on-failure:3"

volumes:
  pg_data:

networks:
  internal:
